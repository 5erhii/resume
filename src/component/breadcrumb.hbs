<ol class='breadcrumb'>
  {{#each breadcrumb}}
    {{#unless url}}
      <li class='breadcrumb-item' aria-current='page'>
        {{name}}
      </li>
    {{/unless}}
    {{#if url}}
      <li class='breadcrumb-item'>
        <a href='{{url}}'>{{name}}</a>
      </li>
    {{/if}}
  {{/each}}
</ol>